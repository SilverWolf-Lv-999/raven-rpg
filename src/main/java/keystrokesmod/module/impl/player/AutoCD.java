package keystrokesmod.module.impl.player;

import keystrokesmod.module.Module;

// 占位，用处是自动菜单
public class AutoCD extends Module {
    final String cdcmd = "/cd";

    public AutoCD(String moduleName, category moduleCategory, int keycode) {
        super(moduleName, moduleCategory, keycode);
    }
}
